<nav class="navbar navbar-expand-lg navbar-dark bg-dark" id="nav">
    <div class="container">
        <NavLink class="navbar-brand" href="/" @onclick="() => SearchState.Clear()"><span class="brand">Movie</span> App</NavLink>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <NavLink class="nav-link" href="/movies" @onclick="() => SearchState.Clear()">Movies</NavLink>
                </li>
                <li class="nav-item">
                    <NavLink class="nav-link" href="/tv-shows" @onclick="() => SearchState.Clear()">TV Shows</NavLink>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Recommendations</a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <NavLink class="dropdown-item" href="/recommendations/movie" @onclick="() => SearchState.Clear()">Movies</NavLink>
                        <NavLink class="dropdown-item" href="/recommendations/tv-show" @onclick="() => SearchState.Clear()">TV Shows</NavLink>
                    </div>
                </li>
                <AuthorizeView>
                    <Authorized>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><i class="fas fa-user-astronaut"></i></a>
                            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                                <NavLink class="dropdown-item" href="@($"/user/profile/{UserId}")" @onclick="() => SearchState.Clear()">Your profile</NavLink>
                                <NavLink class="dropdown-item" href="@($"/lists/{UserId}")" @onclick="() => SearchState.Clear()">Your lists</NavLink>
                                <NavLink class="dropdown-item" href="/user/logout" @onclick="() => SearchState.Clear()">Logout</NavLink>
                            </div>
                        </li>
                    </Authorized>
                    <NotAuthorized>
                        <li class="nav-item">
                            <NavLink class="nav-link" @onclick="HandleRegisterClick">Register</NavLink>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" @onclick="HandleLoginClick">Login</a>
                        </li>
                    </NotAuthorized>
                </AuthorizeView>
            </ul>
            <Search />
        </div>
    </div>
</nav>