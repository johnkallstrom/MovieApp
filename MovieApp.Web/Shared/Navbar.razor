<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-3">
    <div class="container">
        <NavLink class="navbar-brand" href="/" @onclick="@(e => SearchState.SetSearchTerm(string.Empty))">Movie Application</NavLink>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <NavLink class="nav-link" href="/movies" @onclick="@(e => SearchState.SetSearchTerm(string.Empty))">Movies</NavLink>
                </li>
                <li class="nav-item">
                    <NavLink class="nav-link" href="/tv-shows" @onclick="@(e => SearchState.SetSearchTerm(string.Empty))">TV</NavLink>
                </li>
                <li class="nav-item">
                    <NavLink class="nav-link" href="/about" @onclick="@(e => SearchState.SetSearchTerm(string.Empty))">About</NavLink>
                </li>
            </ul>
            <Search />
        </div>
    </div>
</nav>