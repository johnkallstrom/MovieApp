<div id="media-list">
    <p>Your search on '@SearchQuery' resulted in @TotalResults hit(s)</p>
    <div class="row">
        <div class="col-12">
            <ul class="list-group">
                @foreach (var media in Results)
                {
                    <DisplayMedia Media="media" />
                }
            </ul>
        </div>
    </div>
</div>