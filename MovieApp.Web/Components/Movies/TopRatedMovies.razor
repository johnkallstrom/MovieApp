@page "/movies/top-rated" 

<div id="movies-top-rated">
    <h3 class="mb-3">Top Rated Movies</h3>
    <div class="row flex-row flex-nowrap overflow-auto">
        @foreach (var movie in Movies)
        {
            <DisplayMovie Movie="@movie" ShowTitle="false" />
        }
    </div>
</div>