@page "/search"

<div id="search-list">
    <h1>Search</h1>
    <hr />
    @if (!string.IsNullOrEmpty(SearchState.Query))
    {
        <p>Your search on '@SearchState.Query' resulted in @SearchState.TotalResults @DisplayFilter() match(es)</p>
        <div class="row">
            <div class="col-md-3">
                <SearchFilter />
            </div>
            @if (SearchState.Results.Count() is not 0)
            {
                <div class="col-md-9">
                    <ul class="list-group">
                        @foreach (var media in SearchState.Results)
                        {
                            <DisplaySearchMedia Media="media" />
                        }
                    </ul>
                    <SearchPagination />
                </div>
            }
        </div>
    }
</div>